import styles from "./page.module.css";

export default function Page() {
  return (
    <section className={styles["invite-section"]}>
      <div className={styles["invite-container"]}>
        <div className={styles["invite-content"]}>
          <div className={styles["invite-header"]}></div>
          <div className={styles["invite-body"]}>
            {/* Placeholder for invite component */}
          </div>
        </div>
      </div>
    </section>
  );
}
